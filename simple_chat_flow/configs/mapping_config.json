{
    "experiment": {
        "chat_history": "${data.chat_history}",
        "question": "${data.question}"
    },
    "evaluation": {
        "question": "${data.question}",
        "answer": "${run.outputs.answer}"
    }
}
